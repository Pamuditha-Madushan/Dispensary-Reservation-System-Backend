export default class Service {
  constructor(id, name, description, durationMinutes) {
    this.id = id;
    this.name = name;
    this.description = description;
    this.durationMinutes = durationMinutes;
  }
}
